---
import Image from '~/components/common/Image.astro';
import Button from '~/components/ui/Button.astro';
import Fireflies from '~/components/effects/Fireflies.astro';

import type { Hero as Props } from '~/types';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline,

  content = await Astro.slots.render('content'),
  actions = await Astro.slots.render('actions'),
  image = await Astro.slots.render('image'),

  id,
  bg = await Astro.slots.render('bg'),
  class: className = '',
} = Astro.props;
---

<section class:list={["relative md:-mt-[76px] not-prose overflow-hidden min-h-screen md:min-h-[120vh] flex items-center", className]} {...id ? { id } : {}}>
  <div class="absolute inset-0 pointer-events-none" aria-hidden="true">
    <slot name="bg">
      {bg ? <Fragment set:html={bg} /> : null}
    </slot>
    <!-- Fireflies Effect -->
    <Fireflies quantity={15} />
  </div>
  <div class="relative max-w-7xl mx-auto px-4 sm:px-6">
    <div class="pt-0 md:pt-[76px] pointer-events-none"></div>
    <div class="py-16 md:py-20">
      <div class="text-center pb-12 md:pb-16 max-w-5xl mx-auto">
        {
          tagline && (
            <p
              class="text-base text-secondary dark:text-blue-200 font-bold tracking-wide uppercase intersect-once intersect-quarter motion-safe:md:opacity-0 motion-safe:md:intersect:animate-fade"
              set:html={tagline}
            />
          )
        }
        {
          title && (
            <h1
              class="text-4xl md:text-6xl font-bold leading-tight md:leading-tighter tracking-tighter mb-6 font-heading dark:text-gray-200 intersect-once intersect-quarter motion-safe:md:opacity-0 motion-safe:md:intersect:animate-fade"
              set:html={title}
            />
          )
        }
        <div class="max-w-3xl mx-auto">
          {
            subtitle && (
              <p
                class="text-lg md:text-xl text-muted mb-8 dark:text-slate-300 intersect-once intersect-quarter motion-safe:md:opacity-0 motion-safe:md:intersect:animate-fade"
                set:html={subtitle}
              />
            )
          }
          {
            actions && (
              <div class="max-w-xs sm:max-w-md m-auto flex flex-nowrap flex-col sm:flex-row sm:justify-center gap-4 intersect-once intersect-quarter motion-safe:md:opacity-0 motion-safe:md:intersect:animate-fade">
                {Array.isArray(actions) ? (
                  actions.map((action) => (
                    <div class="flex w-full sm:w-auto">
                      <Button {...(action || {})} class="w-full sm:mb-0" />
                    </div>
                  ))
                ) : (
                  <Fragment set:html={actions} />
                )}
              </div>
            )
          }
        </div>
        {content && <Fragment set:html={content} />}
      </div>
      <div id="hero-image-container">
        {
          image && (
            <div class="relative m-auto max-w-5xl mt-8 md:mt-0">
              {typeof image === 'string' ? (
                <Fragment set:html={image} />
              ) : (
                <Image
                  class="mx-auto rounded-lg w-full shadow-2xl"
                  widths={[400, 768, 1024, 2040]}
                  sizes="(max-width: 767px) 400px, (max-width: 1023px) 768px, (max-width: 2039px) 1024px, 2040px"
                  loading="eager"
                  width={1024}
                  height={576}
                  {...image}
                />
              )}
            </div>
          )
        }
      </div>
    </div>
  </div>
</section>

<script>
  import { inView, animate } from 'motion';
  
  // Animar la imagen del hero cuando entra en el viewport
  const heroImage = document.querySelector('#hero-image-container') as HTMLElement;
  
  if (heroImage) {
    // Configurar estado inicial
    heroImage.style.opacity = '0';
    heroImage.style.transform = 'translateY(60px) scale(0.95)';
    heroImage.style.filter = 'blur(8px)';
    
    // Animar cuando entra en el viewport
    inView(heroImage, () => {
      // @ts-expect-error - Motion types
      animate(heroImage, { opacity: 1 }, { duration: 1.2, easing: [0.16, 1, 0.3, 1] });
      // @ts-expect-error - Motion types
      animate(heroImage, { transform: 'translateY(0px) scale(1)' }, { duration: 1.2, easing: [0.16, 1, 0.3, 1] });
      // @ts-expect-error - Motion types
      animate(heroImage, { filter: 'blur(0px)' }, { duration: 1.2, easing: [0.16, 1, 0.3, 1] });
    }, { amount: 0.3 }); // Trigger cuando 30% es visible
  }
</script>
