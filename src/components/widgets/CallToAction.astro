---
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import type { CallToAction, Widget } from '~/types';
import Headline from '~/components/ui/Headline.astro';
import Button from '~/components/ui/Button.astro';

interface Props extends Widget {
  title?: string;
  subtitle?: string;
  tagline?: string;
  callToAction?: CallToAction;
  actions?: string | CallToAction[];
}

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline = await Astro.slots.render('tagline'),
  actions = await Astro.slots.render('actions'),

  id,
  isDark = false,
  classes = {},
  bg = await Astro.slots.render('bg'),
} = Astro.props;
---

<WidgetWrapper id={id} isDark={isDark} containerClass={`max-w-6xl mx-auto ${classes?.container ?? ''}`} bg={bg}>
  <div
    class="relative max-w-3xl mx-auto text-center p-6 rounded-md shadow-xl dark:shadow-none dark:border dark:border-slate-600 overflow-hidden group"
  >
    <!-- Background Image with Zoom Effect -->
    <div 
      class="absolute inset-0 bg-cover bg-[position:80%_100%] z-0 transform scale-110 group-hover:scale-125 transition-transform duration-700 ease-in-out"
      style="background-image: url('/src/assets/backgrounds/uyys3.jpg');"
    ></div>
    
    <!-- Overlay -->
    <div class="absolute inset-0 bg-black/20 z-10"></div>

    <!-- Content -->
    <div class="relative z-20">
      <Headline
        title={title}
        subtitle={subtitle}
        tagline={tagline}
        classes={{
          container: 'mb-0 md:mb-0',
          title: 'text-4xl md:text-4xl font-bold tracking-tighter mb-4 font-heading text-white',
          subtitle: 'text-xl text-white',
        }}
      />
      {
        actions && (
          <div class="max-w-xl m-auto flex flex-wrap justify-center gap-3 mt-6">
            {Array.isArray(actions) ? (
              actions.map((action) => (
                <Button 
                  {...(action || {})} 
                  class="px-6 py-3 text-sm font-semibold bg-white text-commit-dark hover:bg-gray-50 border-2 border-white shadow-lg hover:shadow-xl transition-all duration-300"
                />
              ))
            ) : (
              <Fragment set:html={actions} />
            )}
          </div>
        )
      }
    </div>
  </div>
</WidgetWrapper>
